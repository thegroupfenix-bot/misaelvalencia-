[build]
builder = "nixpacks"
buildCommand = "cd glv-connect/backend && npm install && npm rebuild better-sqlite3"

[deploy]
startCommand = "cd glv-connect/backend && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
healthcheckPath = "/health"
healthcheckTimeout = 30
